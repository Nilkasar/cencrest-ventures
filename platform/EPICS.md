# BeBest Platform — Epic Roadmap (Rebuild)

Branch: `rebuild/platform`. Monorepo: `platform/` (pnpm workspaces + Turborepo). Old `api/`, `web-app/`, and the root marketing site are untouched reference material until their own epics land.

Each epic is built **backend + frontend together** (not backend-first-then-frontend), tracked here and mirrored into `session.md` on completion. No database migration is ever executed against a real database as part of this build — migrations are generated and committed as SQL/Prisma migration files only; applying them is the user's own step.

Status values: `PLANNED` → `SPEC READY` → `IN PROGRESS` → `BUILT (migration pending)` → `VERIFIED (qa-flow-tester)`.

| # | Epic | Domain | Spec | Status |
|---|---|---|---|---|
| 0 | Monorepo & Platform Foundation | Scaffold, schema audit, auth, orgs, RBAC, multi-tenancy, audit logging | [00-backend](docs/epics/00-platform-foundation-backend.md) / [00-frontend](docs/epics/00-platform-foundation-frontend.md) | VERIFIED (migration pending) — flaky @bebest/api test found+fixed during Epic 6/1/2 verification (auth.test.ts NODE_ENV leak) |
| 1 | CRM | Leads, Contacts, Accounts, Deals, Activities, pipeline UI | [spec](docs/epics/01-crm.md) / [backend](docs/epics/01-crm-backend.md) / [frontend](docs/epics/01-crm-frontend.md) / [frontend rewire](docs/epics/01-crm-frontend-rewire.md) | VERIFIED (migration pending) — kanban "move to Lost" reason-capture bug found and fixed; frontend rewired from fixtures to the real API as an Epic 21 follow-up (2026-09-04) — `fixtures.ts` deleted, every screen calls `apiClient` against the real flat routes (`/leads`, `/deals`, `/accounts`, `/activities`), typecheck/lint/build all clean, spot-checked directly. Honest gap surfaced by the rewire, not introduced by it: CRM is gated on a single fixed internal BeBest ops org (`CRM_INTERNAL_ORG_ID`), never per-customer-org-scoped — Epic 18's org switcher now correctly surfaces an actionable error instead of silently faking customer data |
| 2 | Brand Intelligence | Brands, competitors, entities, use cases, brand claims | [spec](docs/epics/02-brand-intelligence.md) / [backend](docs/epics/02-brand-intelligence-backend.md) / [frontend](docs/epics/02-brand-intelligence-frontend.md) | VERIFIED (migration pending) — frontend now wired to the real API, data-contract mismatches resolved against spec text, build-breaking TS error not reproducible/already clean, spot-checked in code |
| 3 | Website Intelligence (Crawler) | Crawl jobs, pages, page issues, sitemaps, SSRF-safe fetch | [spec](docs/epics/03-website-intelligence.md) / [backend](docs/epics/03-website-intelligence-backend.md) / [frontend](docs/epics/03-website-intelligence-frontend.md) | VERIFIED (migration pending) — frontend now wired to real crawl/pages API, enum/field contract fixed, fake progress timer replaced with real incremental polling; no `GET /brands/me/crawl-jobs` list endpoint yet (flagged, minor) |
| 4 | SEO Intelligence | Keywords, keyword groups, SEO analyses, opportunities | [spec](docs/epics/04-seo-intelligence.md) / [backend](docs/epics/04-seo-intelligence-backend.md) / [frontend](docs/epics/04-seo-intelligence-frontend.md) | VERIFIED (migration pending) — built with the new backend-first process, zero wiring gaps, production-ready first pass |
| 5 | Intent & Query Universe | Query sets, queries, intent graph | [spec](docs/epics/05-intent-query-universe.md) / [backend](docs/epics/05-intent-query-universe-backend.md) / [frontend](docs/epics/05-intent-query-universe-frontend.md) | VERIFIED (migration pending) — frontend now wired to the real API, contract mismatch resolved (5 QuerySet fields + Query.source added to the schema, intent_type/category nullability reconciled at the API boundary), both backend bugs fixed (single-active-query-set enforcement on activate, entitlement cap on manual add) with new passing tests, spot-checked in code |
| 6 | AI Provider Abstraction | `AIProvider` interface, Ollama/OpenAI/Anthropic/Google/Perplexity adapters, prompt versioning | [06-ai-provider-abstraction.md](docs/epics/06-ai-provider-abstraction.md) | VERIFIED (qa-flow-tester: production-ready) |
| 7 | AI Visibility Engine (GEO core) | AI runs, ai_responses, brand_observations, AVS formula v1.0 | [spec](docs/epics/07-ai-visibility-engine.md) / [backend](docs/epics/07-ai-visibility-engine-backend.md) / [frontend](docs/epics/07-ai-visibility-engine-frontend.md) | VERIFIED (migration pending) — built with the new backend-first process, zero wiring gaps, production-ready first pass; full evidence drill-down (score -> formula -> observation -> raw response) confirmed working |
| 8 | Competitive Intelligence | Competitor runs/observations, share-of-voice, gap analysis | [spec](docs/epics/08-competitive-intelligence.md) / [backend](docs/epics/08-competitive-intelligence-backend.md) / [frontend](docs/epics/08-competitive-intelligence-frontend.md) | VERIFIED (migration pending) — reuses Epic 7's pipeline for competitor runs (competitor_id on ai_runs, not a parallel pipeline), Share-of-AI-Voice boundary cases tested, evidence sentences match spec format exactly |
| 9 | Opportunity Engine | Unified SEO+GEO scoring, opportunity_evidence | [spec](docs/epics/09-opportunity-engine.md) / [backend](docs/epics/09-opportunity-engine-backend.md) / [frontend](docs/epics/09-opportunity-engine-frontend.md) | VERIFIED (migration pending) — unified score mathematically proven >= max(seoOnly, geoOnly); idempotency, dismiss-then-recompute, and material-change reactivation each independently tested; verify pass caught a genuine cross-epic build-breaking JSDoc bug in Epic 17's code (not Epic 9's), confirmed it, restored the file untouched, and it was independently fixed by the concurrent Epic 17 agent |
| 10 | Recommendation Engine | Recommendations, action_type, effort/impact | [spec](docs/epics/10-recommendation-engine.md) / [backend](docs/epics/10-recommendation-engine-backend.md) / [frontend](docs/epics/10-recommendation-engine-frontend.md) | VERIFIED (migration pending) — dual SEO+GEO requirement proven for all 4 action_types via it.each tests; idempotent generation; priority_rank ordering (score x effort, cheap-outranks-expensive) tested |
| 11 | Content Intelligence & Generation | Content briefs, drafts, approvals, published_content | [spec](docs/epics/11-content-intelligence-generation.md) / [backend](docs/epics/11-content-intelligence-generation-backend.md) / [frontend](docs/epics/11-content-intelligence-generation-frontend.md) | VERIFIED (migration pending) — ADR-007 boundary (no publish anywhere in this epic) proven by grep + an explicit test asserting the response never contains a published field; all 5 quality checks always run and store independently, never a single pass/fail flag; approval screen shows brief+draft side-by-side from one call, never blind |
| 12 | GEO Agent + SEO Agent + Growth Agent | Agent runner, event stream, autonomy levels 1-4 gate | [spec](docs/epics/12-agents.md) / [backend](docs/epics/12-agents-backend.md) / [frontend](docs/epics/12-agents-frontend.md) | VERIFIED (migration pending) — Level 4 hard-blocked at the code level (47-case exhaustive test incl. an `AUTONOMOUS_MODE` env-var proof, never just clamped, spot-checked directly in `autonomy.ts`), reads no config flag that could unlock it; each agent's run() is a thin orchestrator proven via call-order assertions to call Epics 5/7/8/9/10's real functions directly; prompt-injection resistance proven with a falsifiable test (not vacuous); Level 3 approval + 30-day rollback tracked, proven to touch no other table (never executes/publishes) |
| 13 | Action Center & Controlled Publishing | actions table, approval workflow, rollback | [spec](docs/epics/13-action-center-publishing.md) / [backend](docs/epics/13-action-center-publishing-backend.md) / [frontend](docs/epics/13-action-center-publishing-frontend.md) | VERIFIED (migration pending) — approve/execute/rollback guard clauses proven unconditional (Level 4 check fires before the approval check, execute rejects with no approved_at, 30-day rollback window tested at 29d23h/30d1h boundaries); both handoffs (content_drafts and agent_pending_actions to actions) use real unique FKs with idempotency tests, spot-checked directly in code; one post-verify fix landed — pending-approval card now renders the draft body / brief evidence inline, not just a title, per the spec's literal requirement; pre-existing codebase-wide gap noted (authenticated 120/min rate-limit tier defined but never wired into any route) — not introduced by this epic, carried forward to Epic 19 |
| 14 | Measurement & Learning Loop | Re-measurement, before/after attribution | [spec](docs/epics/14-measurement-learning-loop.md) / [backend](docs/epics/14-measurement-learning-loop-backend.md) / [frontend](docs/epics/14-measurement-learning-loop-frontend.md) | VERIFIED (migration pending) — before-score snapshotted immutably at real approval time (Epic 13's approve path), proven with a test that mutates live data post-approval and confirms the snapshot is unaffected; re-measurement calls Epic 7/4's real functions directly (grep-confirmed, no reimplementation); attribution language hedged in both API and UI copy; a real 32-bit setTimeout overflow bug on the 4-week trigger caught and fixed. Only minor, non-epic-specific gaps left (shared rate-limit-tier gap, carried to Epic 19) |
| 15 | Reporting & Notifications | Weekly/monthly digests, in-app + email notifications | [spec](docs/epics/15-reporting-notifications.md) / [backend](docs/epics/15-reporting-notifications-backend.md) / [frontend](docs/epics/15-reporting-notifications-frontend.md) | VERIFIED (migration pending) — immutability proven by a test that drives the real routes, mutates live mock data after generation, and confirms the re-fetched report is byte-identical; weekly digest content traced to real Epic 8/9/14 records/functions, no reimplemented scoring; Epic 12's agent-run-completion and Epic 8's competitor-movement paths both consolidated onto the new shared notify(), zero leftover ad hoc email paths found (independently grepped). Only minor, pre-existing/documented gaps (shared rate-limit-tier gap carried to Epic 19; a client-side notification pagination edge case) |
| 16 | Billing | Plans, subscriptions, entitlements, Stripe adapter | [spec](docs/epics/16-billing.md) / [backend](docs/epics/16-billing-backend.md) / [frontend](docs/epics/16-billing-frontend.md) | VERIFIED (migration pending) — entitlements.ts refactored onto real plans/subscriptions tables (fallback path spot-checked directly in code — only fires when no seeded row exists, not a second hardcoded map), Epics 2/5/7 regression suite confirmed green independently by qa-flow-tester (482/482), PaymentProvider + NullPaymentProvider + HMAC-verified idempotent webhook state machine, cancellation never hard-deletes |
| 17 | Free AI + SEO Snapshot | Public snapshot flow (feeds CRM leads) | [spec](docs/epics/17-free-snapshot.md) / [backend](docs/epics/17-free-snapshot-backend.md) / [frontend](docs/epics/17-free-snapshot-frontend.md) | VERIFIED (migration pending) — rate-limit-before-anything-else and lead-created-before-pipeline both proven via call-order assertions; free-tier scoping (10 pages/50 queries) proven as real threaded parameters, not hardcoded; report token is a SHA-256 hash of a 32-byte random value, never an enumerable DB id; reuses Epic 3's SSRF guard and Epic 4/5/7's real scoring functions directly (no reimplementation) |
| 18 | Agency / White Label / Integrations | Multi-client, white-label, Search Console etc. | [spec](docs/epics/18-agency-white-label-integrations.md) / [backend](docs/epics/18-agency-white-label-integrations-backend.md) / [frontend](docs/epics/18-agency-white-label-integrations-frontend.md) | VERIFIED (migration pending) — RLS never relaxed, only an explicit `agency_clients`-gated authorization check composed on top; revoke-blocks-next-request proven with a genuine two-request regression test (independently re-traced, not rubber-stamped); surfaced the app-wide auth-token-propagation gap below |
| 19 | Production Hardening | Observability, rate-limit durability, load testing | [spec](docs/epics/19-production-hardening.md) / [backend](docs/epics/19-production-hardening-backend.md) / [frontend](docs/epics/19-production-hardening-frontend.md) | VERIFIED — whole-codebase audit+fix pass, exhaustively traced (not sampled) by qa-flow-tester. All 4 `setImmediate` sites centralized behind a new `JobQueue` interface (in-memory default + a real, never-connected pg-boss implementation); the app-wide authenticated-rate-limit-tier gap (flagged by Epics 13/14/15/20) fixed across ~90 call sites; error tracking wired (console default + a real, never-initialized Sentry implementation, provably non-leaking); `GET /brands/me/crawl-jobs` added and the frontend's old localStorage pointer-list workaround fully removed; pagination audited and capped server-side across every list endpoint; `pnpm audit` reduced to zero findings via a verified-safe `deepmerge-ts` override; every app route/screen given a real error boundary (zero existed before this epic). One post-verify fix landed: an end-to-end test now locks in the onError handler's "generic 500, no leak" contract, and a latent unhandled-promise-rejection risk in the job queue's no-handler-registered path was hardened |
| 20 | Marketing Site Rebuild | Root site rebuilt clean on top of the finished platform | [spec](docs/epics/20-marketing-site-rebuild.md) / [frontend](docs/epics/20-marketing-site-rebuild-frontend.md) | VERIFIED — pulled forward to run in parallel with Wave 9 per explicit user request (independent of the rest of the roadmap, touches only root site files + one small backend route). Discovered the live site was already rebranded Cencrest→BeBest (multi-page) on 2026-08-11, never reflected in root CLAUDE.md — root CLAUDE.md rewritten to match reality as part of this epic's fix pass. Apply/contact forms wired to a new public rate-limited POST /api/apply endpoint (real fetch, not alert()); CORS fixed to real production domains; 8-page CTA-to-wrong-destination bug found and fixed. Known gap: `design-bible.html`'s `<title>` still says "Cencrest" (cosmetic, internal-only reference doc, not fixed) |
| 21 | Final Audit | Full done/pending report across every epic above | [report](docs/epics/21-final-audit.html) | DONE — independent, fresh-eyes code-level audit (no prior context on this build, deliberately). Re-ran the full test/build suite personally (995 passed, 0 failed), checked migration consistency (0000-0018, no gaps), traced 3 cross-epic FK chains directly in code, spot-checked 8 EPICS.md claims against current code, and grepped the whole tree for leftover TODOs. Two real findings, both actioned same-day: Epic 1's CRM frontend gap (see row 1 — wiring approved and in progress) and the org-invite email's `console.log`-instead-of-`EmailSender` gap (`routes/orgs.ts`, fixed and tested, suite now at 997 passed). One apparent doc-inconsistency (Epic 20 authorship note) resolved as a timing artifact, not a real contradiction. One false positive in the audit agent's own report (claimed SECURITY.md/TESTING_STRATEGY.md don't exist — they do, at repo-root `docs/`, not `platform/docs/`) caught and corrected before publishing. Report published as a real committed file, not a Claude Artifact — Artifact publishing proved unreliable this session (two publishes silently deleted post-confirmation), reported via feedback |

## Sequencing note

Epics 2–19 above are the honest full roadmap (mirrors the original 37-epic list, consolidated). They will be worked in this order unless a dependency forces reordering (e.g. Epic 6 AI Provider Abstraction must exist before Epic 7 AI Visibility Engine). Each epic gets a short spec written here (or in its own `platform/docs/epics/NN-name.md` once non-trivial) before backend-architect and frontend-engineer start it, from `growth-strategist`.

## Standing rule: every epic spec must include an explicit end-to-end flow checklist

Verifying an epic route-by-route or component-by-component is not enough — it's how integration gaps get missed. **Every `platform/docs/epics/NN-*.md` spec must include a numbered "End-to-end flow" section** tracing the real sequence of steps a user or the system goes through, naming every handoff (API call → DB write → UI update → notification → audit log, etc.). `qa-flow-tester` must walk that exact numbered list — not just each piece in isolation — before an epic can move to `VERIFIED`. Specs written before this rule (01, 02, 06) have had the section retrofitted; every spec from 07 onward includes it from the start.

## Standing rule: backend builds first, frontend wires to the REAL API — no parallel fixture-building

Wave 1 (Epic 2) and Wave 2 (Epics 3 and 5) all independently produced the identical failure: a solid, well-tested backend and a solid, well-designed frontend, built in parallel against each side's own invented fixtures/types, that were never actually wired together and disagreed on field names/enums when checked. Running backend and frontend fully in parallel from a bare spec is what causes this — each side has to guess the other's exact contract instead of reading it.

**Fix, effective from Wave 3 onward**: within an epic, the backend agent runs to completion FIRST. The frontend agent then starts by reading the actual deployed routes/response shapes/Prisma fields (not the spec's prose, which is necessarily looser) and wires directly against them from the first line of data-layer code — no localStorage/in-memory fixture layer as the "real" data source, even temporarily. This costs some wall-clock parallelism (epics no longer build backend+frontend simultaneously) but eliminates this entire class of bug at the source rather than catching and fixing it after the fact every time.
